import React from 'react';

function Counter(props) {
  return (
      <div>
        Counter: {props.count}
        {/*console.log("Counter: ",props.count)*/}
      </div>
  );
}

export default Counter;
